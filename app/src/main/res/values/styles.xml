<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="editTextStyle">
        <item name="android:colorControlActivated">@color/ecoseMainWhite</item>
        <item name="android:colorControlHighlight">@color/ecoseMainWhite</item>
        <item name="android:colorControlNormal">@color/ecoseMainWhite</item>
        <item name="android:colorAccent">@color/ecoseMainWhite</item>
    </style>

</resources>
